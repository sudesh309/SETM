[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "setm_core"
version = "0.1.0"
description = "Optional Rust acceleration for SETM graph traversal"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
